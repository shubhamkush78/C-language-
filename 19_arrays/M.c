#include <stdio.h>

int main() {

    int x[2][3] = {{1, 2, 3}, {11, 22, 33}};

    int i, j;

    int row_len = sizeof(x) / sizeof(x[0]);
    int col_len = sizeof(x[0]) / sizeof(int);

    int y[][];

    //----


    //===

    return 0;
}