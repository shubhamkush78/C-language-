#include <stdio.h>

int main() {

    int x = 66;

    printf("%d %c", x, x);  
    // conversion using conversion specifier(format specifier)
    // conversion is specified explicitly ...

    return 0;
}