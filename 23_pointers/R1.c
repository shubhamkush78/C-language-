#include <stdio.h>
#include <string.h>

int main() {

    char *name = "vikramaditya kumar";

    printf("pointer(name) byte size: %d and string(vikramaditya kumar) length: %d", sizeof(name), strlen(name));

    return 0;
}

