#include <stdio.h>
#include <string.h>

int main() {

    char name[15] = "vikramaditya kumar";

    printf("Array byte size(name) length: %d and string(vikramaditya kumar) length: %d", sizeof(name), strlen(name));

    return 0;
}

